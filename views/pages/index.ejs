<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/header.ejs %>
</head>
<body>
    <div id="welcomeDiv" class="visible">
        <% if (!user) { %>
            <p>Welcome! Please 
                <input id="loginBtn" type="button" value="Log In"/> <!--this should trigger eventlistener to change loginDiv to visible from hidden-->
            </p>            
        <% } else { %>
            <p>
                Hello, <%= user.name %>. Click <a href="/main">Here</a> 
                to View the database
                <!--reroute to db page-->
            </p>
            <p>
                Or view your <input type="button" id="profileBtn" value="Profile"/>
                <!--trigger eventlistener to change profileDiv to visible from hidden-->
            </p>
        <% } %>
    </div>
    <div id="loginDiv" class="hidden">
        <% include ../partials/login.ejs %>
    </div>
    <div id="profileDiv" class="hidden">
        <% include ../partials/profile.ejs %>
    </div>
    <script src="../../public/main.js"></script>
</body>
</html>